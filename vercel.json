{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "nextjs",
  "buildCommand": "prisma migrate deploy && npm run build",
  "regions": ["gru1"],
  "crons": [{ "path": "/cron/fila", "schedule": "0 * * * *" }]
}
